---
title: Getting started
description: Install Conclave and run the Day-0 founding-artifacts flow in a few minutes.
---

# Getting started

## Three commands to get going

Inside any git repo:

```bash
# 1. Bootstrap the conclave/ workspace
/conclave-init

# 2. Generate the founding artifacts from your product idea
/conclave-spec "REST API for task management with JWT auth"

# 3. Lock Sprint 1
/conclave-planning
```

That's it. After step 3 the sprint is `active`, every story has an assignee and a discipline, and the team can open a PR with the entire `conclave/` directory.

`/conclave-init` asks up front whether this is a solo project or a team. Solo mode forces the `lean` profile and renders a single-person roster covering every discipline; team mode asks who covers each of the six disciplines (name + GitHub handle, or `TBD` if unstaffed).

## What gets created

After `/conclave-spec` runs, your repo gains a `conclave/` directory at the root:

```
conclave/
├── README.md
├── config.md                         # team_mode, team_profile, ceremony flags
├── team/
│   ├── roster.md                     # who covers which discipline, plus optional PM/SM
│   └── ceremonies.md
├── product/
│   ├── backlog.md                    # ordered Product Backlog
│   ├── architecture.md               # ADRs + mermaid diagram
│   ├── definition-of-ready.md
│   └── definition-of-done.md
├── context/                          # frozen snapshots of inputs used
└── sprints/
    └── SPRINT-001/
        ├── meta.md
        ├── spec.md                   # sprint plan
        ├── stories/                  # one file per user story
        └── acceptance/                # Gherkin acceptance criteria
```

Every file is markdown. Every change is a normal git diff your team can review in a PR.

## Continuing the loop

Once Sprint 1 is locked:

```bash
# Each dev picks up an assigned story
/conclave-dev US-001

# QA verifies the story when it reaches review
/conclave-qa US-001

# Tech Lead approves the PR (only when peer_pr_review.required is on)
/conclave-pr-review US-001
```

When all the sprint's stories are `done`, you're ready for the next planning cycle. Re-run `/conclave-spec` to groom more of the idea into the backlog, or move directly to `/conclave-planning` for `SPRINT-002`.

## Next steps

- See a [team example](/en/team-example) — how several developers, each on their own local Claude Code session, actually work a sprint together.
- Read the [methodology](/en/methodology) to understand the Scrum model Conclave assumes.
- Pick the right [team profile](/en/profiles) for your situation.
- Check the [configuration reference](/en/configuration) for every field in `conclave/config.md`.
- Dig into the [per-command reference](/en/commands/init).

---
id: "{{sprint_id}}"
title: "{{sprint_title}}"
status: draft               # draft | active | done | archived
created_at: "{{iso_date}}"
target_start: "{{start_date}}"
target_end: "{{end_date}}"
context_snapshot: conclave/context/
generated_by: conclave
---

# {{sprint_id}}: {{sprint_title}}

## Goal

{{sprint_goal}}

## Stories included

- US-{{id_1}}
- US-{{id_2}}
- US-{{id_3}}

## Status transitions

- `draft` — generated by `/conclave-spec`; not yet locked.
- `active` — locked by `/conclave-planning` (planned, not yet shipped); the team is working on it.
- `done` — closed by `/conclave-close-sprint` (planned, not yet shipped) after Review.
- `archived` — historical; no further edits expected.

## Audit trail

The context snapshot at `conclave/context/` reflects the state of `CLAUDE.md`, available Skills, and detected project rules at the moment this sprint was generated. Compare against the current state when re-grooming to see what changed.

# {{project}} — Sprint Board

Static roadmap board generated by Conclave's `/conclave-sprint-board`.

**Snapshot:** `{{snapshot_iso}}`  
**Accent source:** `{{accent_source}}`

## Open

```bash
open docs/sprint-board/index.html          # macOS
xdg-open docs/sprint-board/index.html      # Linux
# Or double-click index.html — works offline via file://
```

If the browser restricts `file://`:

```bash
cd docs/sprint-board && python3 -m http.server 8080
# then open http://localhost:8080
```

## Refresh

This HTML is a **derived snapshot**. The source of truth is still `conclave/`.

```text
/conclave-sprint-board
```

Re-running overwrites `index.html` and this README. It does **not** change story or sprint markdown.

## Related

- Status Kanban (Next.js): `/conclave-board` → `conclave-board/`
- This board: roadmap / tasks / analytics (offline HTML)
